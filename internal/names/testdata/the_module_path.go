// Fixture. The module path carries the organisation as a domain. It is not an
// account name and refusing it would turn this check off on its first run.
package fixture

import _ "an-account.dev/hub/internal/sources"

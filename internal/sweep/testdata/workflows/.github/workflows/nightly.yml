# A workflow that runs on a schedule and on request. It is the ordinary case.
name: Nightly

on:
  schedule:
    - cron: "17 5 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  nightly:
    name: Nightly
    runs-on: ubuntu-latest
    steps:
      - run: echo done
